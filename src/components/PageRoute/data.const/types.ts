export type TSectionHeader = {
  id: string
  title: string
  owner: string
  intro: React.ReactNode
  image: React.ReactNode
}

export type TSectionDetails = {
  id: string
  title: string
  body: React.ReactNode
  no: number
}
